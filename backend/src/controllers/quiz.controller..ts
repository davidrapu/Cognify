import {Request, Response, NextFunction} from "express";

async function getQuizData(req: Request, res: Response, next: NextFunction) {
  // Implementation for fetching quiz data
}

module.exports = {
  getQuizData,
};