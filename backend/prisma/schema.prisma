// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../src/database/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(uuid())
  firstName String
  lastName  String
  email     String   @unique
  hash      String   
  session   GameSession[]
  role      Role     @default(USER)
  refreshTokens RefreshToken[]
  createdAt DateTime @default(now())
}

model GameSession {
  id              Int @id @default(autoincrement())
  userId          String
  gameName        GameName
  correct         Int
  incorrect       Int
  accuracy        Float
  gameScore      Float
  reactionScore   Float
  reactionTimeAvg Float
  reactionTimeStd Float
  duration        Float
  domain          Domain
  createdAt       DateTime @default(now())
  // Define the relations to the User model
  user            User   @relation(fields: [userId], references: [id])
}

model RefreshToken {
  id        Int    @id @default(autoincrement())
  userId    String
  token     String @unique
  createdAt DateTime @default(now())
  // Define the relations to the User model
  user      User   @relation(fields: [userId], references: [id])
}

enum Role {
  USER
  ADMIN
}

enum Domain {
  MEMORY
  ATTENTION
  PROCESSING_SPEED
  EXECUTIVE_FUNCTIONS
  REASONING
}

enum GameName {
  DIGITAL_SPAN
  CARD_MATCH
  SEQUENCE_RECALL
  VISUAL_SEARCH
  STROOP_TEST
  GO_NO_GO
  CHOICE_REACTION_TIME
  PATTERN_RECOGNITION
  ARITHMETIC_PATTERN_RECOGNITION
}